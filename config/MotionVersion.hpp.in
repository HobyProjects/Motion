// Version.h - Auto-generated version information
// DO NOT EDIT THIS FILE MANUALLY - Generated by CMake

#pragma once

#include <string>

namespace Motion 
{
    class Version 
    {
        public:
            // Version components
            static constexpr int Major = @VERSION_MAJOR@;
            static constexpr int Minor = @VERSION_MINOR@;
            static constexpr int Patch = @VERSION_PATCH@;
            
            // Full version string (e.g., "1.0.0-a1b2c3d" or "1.0.0-a1b2c3d-dirty")
            static constexpr const char* String = "@VERSION_STRING@";
            
            // Git information
            static constexpr const char* GitCommitHash = "@GIT_COMMIT_HASH@";
            static constexpr const char* GitBranch = "@GIT_BRANCH@";
            static constexpr bool GitDirty = @GIT_DIRTY@;
            
            // Build information
            static constexpr const char* BuildTimestamp = "@BUILD_TIMESTAMP@";
            static constexpr const char* BuildType = "@BUILD_TYPE@";
            
            // Formatted strings
            static std::string GetVersionString() {
                return String;
            }
            
            static std::string GetFullVersionInfo() {
                std::string info = "Motion Engine v" + std::string(String);
                info += "\nBuild: " + std::string(BuildType);
                info += "\nBuilt: " + std::string(BuildTimestamp);
                if (std::string(GitCommitHash) != "unknown") {
                    info += "\nCommit: " + std::string(GitCommitHash);
                    info += "\nBranch: " + std::string(GitBranch);
                    if (GitDirty) {
                        info += " (uncommitted changes)";
                    }
                }
                return info;
            }
            
            static std::string GetShortVersionInfo() {
                return "Motion Engine v" + std::string(String);
            }
    };

} // namespace Motion
